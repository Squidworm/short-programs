#Function that takes in a list of numbers, 
#removes all odd numbers in the list, and returns the result.

def purify(number_list):
    even_list = []
    for item in number_list:
        if item%2 == 0:
            even_list.append(item)
    return even_list

print(purify([3,4,5,2,3,4,5,4,6,6,4]))
